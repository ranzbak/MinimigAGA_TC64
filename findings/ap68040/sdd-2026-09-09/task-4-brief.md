# Task 4: D1 -- the bench enforces the chip-select contract, then the five-phase enable behind it

Plan: findings/ap68040/plan-v2-with-ddr3.md, "Stage D — REVISED", paragraph "D1 is written but does not boot, and the bisect is done", and the note "D1 has a prerequisite option 1a does not mention". Read both before starting. Also memory of the bench traps: the bench does not compile sdram_ctrl and models the enable cadence itself.

## State of the tree
- rtl/sdram/sdram_ctrl.v: FOUR enable phases (ph2/6/10/14) -- commit 2f962f1 reverted D1's five while keeping the latched chipset release (`chipset_done` in TG68K.vhd). This boots on hardware.
- sim/ddr3_cpu/ddr3_cpu_tb.sv ~line 181-203: still pulses FIVE phases (2/5/8/11/14). Bench and RTL disagree today.
- fpga/openaars/aars_v5.0/xc7a100t/cpu.xdc: kernel island at -setup -start 3 / -hold -start 2 (D1's values; tighter than four phases need, harmless, leave it).
- rtl/soc/TG68K.vhd ~line 934-942: `slower` reloads "0111" on every enable and shifts right each clock; `ramcs`/`ddrcs` (lines 534-537) are held off by slower(0). The untested hypothesis: at three-cycle spacing the select opens on the very cycle the next enable can release the CPU, breaking the controllers' contract "cpuAddr must be stable ONE CYCLE BEFORE cpustate[2] goes low" (rtl/ddr3/ddr3_fastram.v header, and rtl/sdram/sdram_ctrl.v -- read both headers and state the exact contract, including what must hold while the select is low and after cpuena).

## Steps
1. **One cadence source.** Move the enable cadence out of sdram_ctrl's case statement and the tb's counter into ONE small combinational module (e.g. rtl/sdram/cpu_enable_cadence.v: phase[3:0] in, enaWRreg/ena7RDreg/ena7WRreg next-values out, phase list in one place, four-phase list today). sdram_ctrl.v registers its outputs exactly where it registers them now; the tb instantiates the same module on its phase counter. Add the file to every source list that carries sdram_ctrl or the tb (tools/vivado/build.tcl, sim/ddr3_cpu/run.sh, the Vivado project's fileset if build.tcl does not add it -- check how sdram_ctrl.v itself is listed). Gate: plain `./run.sh` and `./run.sh --ap040` PASS with the bench now at FOUR phases (phase timestamps must move relative to the five-phase logs in the workspace dir -- if they do not, the bench is not using the module).
2. **The contract assertion.** In ddr3_cpu_tb.sv, for BOTH RAM ports (the SDRAM-side model port and the DDR3 port tg68_ddrcs/tg68_ddraddr): on the clock where the select goes low, the address must equal the address of the previous clock; plus whatever stability the controller headers demand while the select is low. Make it a FAIL that trips the summary line (the mutant guard tests the summary line, not the program's own verdict). Gate: four-phase runs stay clean.
3. **Five phases, RTL and bench together** (edit only the cadence module: ph 2/5/8/11/14). Run `./run.sh --ap040`. EXPECTED: the new assertion fires. If it does NOT fire and the run passes, the hypothesis is refuted -- stop here, leave the cadence at four phases, and report DONE_WITH_CONCERNS with what you saw; do not invent another fix.
4. If it fires: change the `slower` reload from "0111" to "0011" (TG68K.vhd ~line 940), re-run. Then the full set, one at a time: `./run.sh --ap040`, `--ap040 --chipbus`, `--mmu`, `--lwmutant` (MUST fail), `--mmumutant` (MUST fail), plain `./run.sh` (TG68K control -- sdram_ctrl is shared, so the TG68K sees five phases too and must PASS). Record phase timestamps against the four-phase logs from step 1.
5. Plan doc: a Log paragraph with the outcome (hypothesis confirmed/refuted, the numbers), and update the D1 row's status text. Commit RTL + tb + lists + plan. Do not stage Paul's pre-existing edits (fw/ctrl_832/version.h, sim/hostcpu-i2c-bridge/*.wcfg, tools/vivado/ila_fastram_check.py) or untracked scratch.

## Exit criterion
Cadence has one source shared by RTL and bench; the contract assertion exists and catches the five-phase/"0111" combination (or the report says it does not, with evidence); if the fix holds, all six runs behave as required with five phases. No bitstream in this task (Task 5).

## Addendum (controller, 05:40, after Task 3 landed)
- Step 2 (the address/select contract assertion on both ports) was ALREADY DONE in Task 3: sim/ddr3_cpu/ddr3_cpu_tb.sv ~:694-734 (rising edge of the select only). Do not duplicate it; extend it if the five-phase experiment needs the "address holds while the select is low" half (the controllers re-register cpuAddr_r every cycle, so that half matters) -- add it, and say so.
- The clkena expression in TG68K.vhd is now `clkena_in = '1' AND (bstate = "01" OR bus_ready = '1')` (commit 727e4f4 removed the walker/fill ack terms; the bench asserts the invariant: ack_idle_errs / ack_dry_errs). Keep it that way; the five-phase change must not put terms back on that net (it is the CE path of 7,391 kernel flops and was failing timing last night).
- The fill router (fl_* FSM, Task 3) also drives the RAM ports and paces on mem_ready, and `slower` gates its selects exactly as the CPU's. A `slower` reload change affects it too; --ap040 with its fill counters (83 channel fills, 1 auto-completed, 0 bus errors expected) and --fillmutant (must fail) are part of your gate now, in addition to the six legs in the brief. That makes eight legs; run them one at a time; ~25 min each.
- The bench had never enabled the 040's internal caches before Task 3 (CACR); run.sh now passes -DCACRVAL=$80008003 for AP040 legs. Keep that.
- Builds read the working tree: do NOT run Vivado while editing; no bitstream in this task (the controller builds after your commit).
